import { artsService } from "@/fsd/shared/api";
import { useMutation } from "react-query";

// export const useGetTagMutation = () => {
//   return useMutation({
//     mutationKey: ["getTags"],
//     mutationFn: (tagName: string) => artsService.getTags({ tagName }),
//   });
// };
